package com.inlaco.crewmgrservice.feature.post.service.impl;

import org.springframework.stereotype.Repository;

@Repository
public class CustomPostRepository {

  // public Page<Post> findAtivePosts(

  //
  //   Pageable pageable) {
  //   return postRepository.findByAuthorId(new ObjectId(authorId), pageable);
  // }

}
